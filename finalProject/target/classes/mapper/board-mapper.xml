<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="boardMapper">

	<select id="boardList" resultType="Board">
		select * from second_goods
		where SG_STATUS='N' AND B_CATEGORY='A'
		order by SG_ID desc
	</select>
	
	<select id="getAuctionListCount" resultType="_int">
		select count(*) from second_goods where sg_status='N' and B_CATEGORY='A'
	</select>
	
	<select id="boardEndTimeList" resultType="Board">
		select SG.*,FLOOR(SG_END_DATE-SYSDATE) AS "ENDTIME" 
		from second_goods SG
		WHERE SG_STATUS='N' AND B_CATEGORY='A'
		ORDER BY FLOOR(SG_END_DATE-SYSDATE)
	</select>
	
	<select id="boardFileList" resultType="Attachment">
		SELECT * FROM ADD_FILE
		WHERE FILE_STATUS='N'
	</select>
	
	<select id="getSecondGoodListCount" resultType="_int">
		SELECT COUNT(*) FROM SECOND_GOODS
		WHERE SG_STATUS='N' AND B_CATEGORY='SG'
	</select>

	<select id="secondGoodBoardList" resultType="Board">
		SELECT * FROM SECOND_GOODS
		WHERE SG_STATUS='N' AND B_CATEGORY='SG'
		ORDER BY SG_ID DESC
	</select>

</mapper>
